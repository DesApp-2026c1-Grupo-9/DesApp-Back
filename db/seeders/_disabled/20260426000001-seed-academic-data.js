'use strict';

// Seeder dejado intencionalmente vacío.
// Reemplazado por 20260426000001-seed-complete-academic-data.js
module.exports = {
  up: async (queryInterface, Sequelize) => {},
  down: async (queryInterface, Sequelize) => {},
};
